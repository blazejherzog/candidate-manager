<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.candidate-manager</groupId>
        <artifactId>candidate-manager</artifactId>
        <version>0.0.1-SNAPSHOT</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <artifactId>cm-app</artifactId>
    <packaging>jar</packaging>

    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-oauth2-resource-server</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>

        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
        </dependency>

        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>

        <dependency>
            <groupId>com.candidate-manager</groupId>
            <artifactId>cm-shared</artifactId>
        </dependency>
        <dependency>
            <groupId>com.candidate-manager</groupId>
            <artifactId>cm-auth</artifactId>
        </dependency>
        <dependency>
            <groupId>com.candidate-manager</groupId>
            <artifactId>cm-candidate</artifactId>
        </dependency>
        <dependency>
            <groupId>com.candidate-manager</groupId>
            <artifactId>cm-job</artifactId>
        </dependency>
        <dependency>
            <groupId>com.candidate-manager</groupId>
            <artifactId>cm-resume</artifactId>
        </dependency>
        <dependency>
            <groupId>com.candidate-manager</groupId>
            <artifactId>cm-event</artifactId>
        </dependency>
        <dependency>
            <groupId>com.candidate-manager</groupId>
            <artifactId>cm-integration</artifactId>
        </dependency>
    </dependencies>
</project>